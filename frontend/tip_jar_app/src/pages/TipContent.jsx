import React from 'react';
import SendTip from '../components/SendTip';

const TipContent = () => {
    return (
        <div>
            <SendTip />
        </div>
    );
};

export default TipContent;
